syntax = "proto3";
package dos;

message Card {
	int32 number = 1;
	CardType type = 2;
	CardColor color = 3;
}

enum CardType {
	NORMAL = 0;
	SKIP = 1;
	DOUBLEDRAW = 2;
	REVERSE = 3;
	WILD = 4;
	QUADDRAW = 5;
}

enum CardColor {
	RED = 0;
	YELLOW = 1;
	GREEN = 2;
	BLUE = 3;
	BLACK = 4;
}

