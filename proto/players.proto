syntax = "proto3";
package dos;

// Messages sent from server to client after handshake but before game starts.
// Contains information to synchronize player lists.
message PlayersMessage {
	repeated string additions = 1;
	repeated string deletions = 2;
}

